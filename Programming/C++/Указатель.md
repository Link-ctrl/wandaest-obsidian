---
создал заметку: 21-04-2025
tags:
  - База
---
### Описание
🔹 **Указатель** — это **переменная, которая хранит адрес другой переменной** (или функции, или объекта).
📌 Указатель:
- знает **где находится объект**
- позволяет **читать и/или менять** этот объект по адресу
- может быть **нулевым** (`nullptr`)

Пример:
```cpp
int x = 10;
int* ptr = &x;

std::cout << *ptr << "\n";  // → 10 (разыменование)
```
- `ptr` — это **указатель на int**
- `&x` — **адрес переменной x**
- `*ptr` — **разыменование** (доступ к значению по адресу)

### Сколько байт занимает указатель?

> **Указатель занимает 4 байта на 32-битной системе**  
> **и 8 байт на 64-битной системе**

|                         | Указатель (`T*`) | Ссылка (`T&`)  |
| ----------------------- | ---------------- | -------------- |
| Может быть `nullptr`?   | ✅ Да             | ❌ Нет          |
| Можно переназначить?    | ✅ Да             | ❌ Нет          |
| Разыменовывается явно?  | ✅ `*ptr`         | ❌ Просто `ref` |
| Используется в массивах | ✅ Да             | ❌ Обычно нет   |
### Резюме
> **Указатель в C++ — это переменная, содержащая адрес**  
> другого объекта или функции. Он занимает:
> - **4 байта на 32-битных системах**
> - **8 байт на 64-битных**

📌 Это ключевой механизм для работы с динамической памятью, массивами, функциями и ООП.